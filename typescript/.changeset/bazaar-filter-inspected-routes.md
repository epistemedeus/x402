---
"@x402/extensions": minor
---

Add `filterDiscoveryResources` to keep Bazaar `listResources` / `search` rows whose resource URL matches a verified inspected-route document (`origin` + `route`). Remaining rows keep their original order; facilitator ranking is unchanged.
